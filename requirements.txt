# Enhanced Bathymetric CAE Dependencies
# 
# IMPORTANT: For best GDAL support, use conda environment:
#   conda env create -f environment.yml
#   conda activate bathymetric-cae
#   pip install -e .
#
# The requirements below are for pip-only installations
# and may have GDAL compatibility issues.

# Core ML and Scientific Computing
tensorflow>=2.13.0
numpy>=1.21.0
scipy>=1.7.0
scikit-learn>=1.0.0
scikit-image>=0.19.0

# Geospatial and Data Processing
# NOTE: These packages work better when installed via conda-forge:
# - gdal>=3.4.0
# - rasterio>=1.3.0
# - opencv-python>=4.5.0
# If using pip, install system GDAL first:
# Ubuntu: sudo apt install gdal-bin libgdal-dev
# macOS: brew install gdal
# Windows: Use conda or OSGeo4W

# For pip-only installation (less reliable):
# GDAL>=3.4.0  # Uncomment if conda not available
# rasterio>=1.3.0  # Uncomment if conda not available
opencv-python>=4.5.0

# Visualization
matplotlib>=3.5.0
seaborn>=0.11.0

# Utilities
joblib>=1.1.0
psutil>=5.8.0
pathlib2>=2.3.0

# Optional GPU support
# tensorflow[and-cuda]>=2.13.0  # Uncomment for CUDA GPU support

# Optional hydrographic tools (conda recommended)
# pyproj>=3.3.0      # Better via conda-forge
# fiona>=1.8.0       # Better via conda-forge  
# geopandas>=0.12.0  # Better via conda-forge